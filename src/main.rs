//! sierpinsky triangle image generator

extern crate image;
extern crate rand;

use rand::Rng;
use std::fs::File;
use std::path::Path;

/// points used for building triangle and plot on canvas
pub struct Point{
    x: u32,
    y: u32
}

/// main program
pub fn main() {

}
